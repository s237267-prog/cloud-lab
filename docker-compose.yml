version: '3.8'

services:
  backend:
    build: ./mern-demo/server
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - MONGODB_URI=mongodb+srv://s237267_db_user:12345@cluster0.vmhiuly.mongodb.net/mernDB?retryWrites=true&w=majority
    restart: always

  frontend:
    build: ./mern-demo/client
    ports:
      - "3000:3000"
    depends_on:
      - backend
    restart: always
